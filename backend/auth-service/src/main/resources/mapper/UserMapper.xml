<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learn.auth.mapper.UserMapper">

    <resultMap id="userEntityResultMap" type="com.learn.auth.entity.UserEntity">
        <id property="id"
            column="id"
            javaType="java.util.UUID"
            jdbcType="OTHER"
            typeHandler="com.learn.auth.mybatis.UuidTypeHandler"
        />
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="credits" column="credits"/>
        <result property="status" column="status"/>
        <result property="tokenVersion" column="token_version"/>
        <result property="emailVerifiedAt" column="email_verified_at"/>
        <result property="avatarBucket" column="avatar_bucket"/>
        <result property="avatarObjectKey" column="avatar_object_key"/>
        <result property="avatarOriginalName" column="avatar_original_name"/>
        <result property="avatarContentType" column="avatar_content_type"/>
        <result property="avatarSize" column="avatar_size"/>
        <result property="avatarSha256" column="avatar_sha256"/>
        <result property="avatarUpdatedAt" column="avatar_updated_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByEmail" resultMap="userEntityResultMap">
        SELECT id,
               name,
               email,
               password_hash,
               credits,
               status,
               token_version,
               email_verified_at,
               avatar_bucket,
               avatar_object_key,
               avatar_original_name,
               avatar_content_type,
               avatar_size,
               avatar_sha256,
               avatar_updated_at,
               created_at,
               updated_at
        FROM users
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="userEntityResultMap">
        SELECT id,
               name,
               email,
               password_hash,
               credits,
               status,
               token_version,
               email_verified_at,
               avatar_bucket,
               avatar_object_key,
               avatar_original_name,
               avatar_content_type,
               avatar_size,
               avatar_sha256,
               avatar_updated_at,
               created_at,
               updated_at
        FROM users
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.learn.auth.entity.UserEntity">
        INSERT INTO users (
            id,
            name,
            email,
            password_hash,
            credits,
            status,
            token_version
        ) VALUES (
            #{id},
            #{name},
            #{email},
            #{passwordHash},
            #{credits},
            #{status},
            #{tokenVersion}
        )
    </insert>

    <update id="updateUserInfoById">
        UPDATE users
        <set>
            <if test="userDTO.name != null">
                name = #{userDTO.name},
            </if>
            <if test="userDTO.email != null">
                email = #{userDTO.email},
                email_verified_at = NULL,
            </if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
          AND status = 'ACTIVE'
    </update>

    <update id="updateAvatarById">
        UPDATE users
        SET avatar_bucket = #{avatar.bucket},
            avatar_object_key = #{avatar.objectKey},
            avatar_original_name = #{avatar.originalName},
            avatar_content_type = #{avatar.contentType},
            avatar_size = #{avatar.size},
            avatar_sha256 = #{avatar.sha256},
            avatar_updated_at = #{avatar.updatedAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND status = 'ACTIVE'
    </update>

</mapper>
